Installing the `river-route` package exposes the `rr` CLI command.

```
usage: rr [-h] [-c CONFIG]

CLI for the river_route Python package

options:
  -h, --help            show this help message and exit
  -c CONFIG, --config CONFIG
                        path to routing configuration file
```