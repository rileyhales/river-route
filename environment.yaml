name: rr
channels:
  - conda-forge
dependencies:
  - python>=3.12
  - geopandas
  - netcdf4
  - networkx
  - numba
  - numpy
  - pandas
  - pyyaml
  - scipy
  - shapely
  - tqdm
  - xarray
  # enable optional backends/features in xarray
  - dask
  # for tests
  - pytest
  # in development
  - pandas-stubs
  - scipy-stubs
  - setuptools
  # for documentation
  - mkdocs<2
  - mkdocs-material
  - mkdocstrings
  - mkdocstrings-python
  - mkdocs-include-markdown-plugin
